name: Docker Image CI
on: 
  push:
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v3
    - name: get number
      id: getnumber
      run: |
        # wget https://download.vroid.com/dist/VRoidStudio-v0.14.0-win.exe
        echo "asdsasd" > as.txt
        zip -r pkg.zip *.txt
        echo "$(openssl rand -hex 4)" > "random-name-$(openssl rand -hex 4).txt"
        echo "$(openssl rand -hex 4)" > "random-name-$(openssl rand -hex 4).txt"
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: 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 # This token is provided by Actions, you do not need to create your own token
      with:
        tag_name: majeksijfisfsd
        release_name: Release majeksijfisfsd
        body: |
          Test upload-to-github-release works with ```actions/create-release```
        draft: true
        prerelease: false
    - uses: xresloader/upload-to-github-release@master
      env:
        GITHUB_TOKEN: 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
      with:
        file: "*.md;*.zip;random-name-*.txt"
        delete_file: "random-name-*.txt;random-*.txt"
        release_id: ${{ steps.create_release.outputs.id }}
        overwrite: true
        verbose: true
